# XcodeGen project.yml
# https://github.com/yonaskolb/XcodeGen
#
# To generate the Xcode project:
#   1. Install XcodeGen: `brew install xcodegen`
#   2. Run: `xcodegen generate`
#   3. Open VoiceFlow.xcodeproj
#
# This makes the project structure 100% reproducible and code-reviewable.

name: VoiceFlow
options:
  bundleIdPrefix: de.marvinschwab
  deploymentTarget:
    iOS: "26.0"
  createIntermediateGroups: true
  generateEmptyDirectories: true
  groupSortPosition: top

settings:
  base:
    SWIFT_VERSION: "5.9"
    IPHONEOS_DEPLOYMENT_TARGET: "26.0"
    ENABLE_USER_SCRIPT_SANDBOXING: YES
    # Strict concurrency is OFF by default for simulator-friendly builds.
    # The codebase still uses @MainActor / Sendable correctly; we just don't
    # promote every concurrency warning to an error so first-time builds
    # (especially in Xcode iPhone Simulator) succeed without surprise.
    # Re-enable per-target or per-scheme when you're ready to harden:
    #   SWIFT_STRICT_CONCURRENCY: complete
    #   SWIFT_TREAT_WARNINGS_AS_ERRORS: YES
    SWIFT_STRICT_CONCURRENCY: minimal
    SWIFT_TREAT_WARNINGS_AS_ERRORS: NO
    GCC_TREAT_WARNINGS_AS_ERRORS: NO
    DEVELOPMENT_TEAM: ""
    CODE_SIGN_STYLE: Automatic
    SWIFT_UPCOMING_FEATURE_REGION_BASED_ISOLATION: NO

# Package dependencies (SPM)
packages:
  WhisperKit:
    url: https://github.com/argmaxinc/WhisperKit
    from: "0.7.0"
  # FluidAudio:
  #   url: https://github.com/FluidInference/FluidAudio
  #   from: "0.1.0"

# ============================================================================
# Targets
# ============================================================================

targets:

  # ==========================================================================
  # Main App Target
  # ==========================================================================
  VoiceFlow:
    type: application
    platform: iOS
    sources:
      - path: Sources/VoiceFlow
      - path: Sources/VoiceFlowShared
    resources:
      - path: Resources
        optional: true
    dependencies:
      - target: VoiceFlowShared
        embed: true
      - package: WhisperKit
    info:
      path: Sources/VoiceFlow/Info.plist
      properties:
        CFBundleDisplayName: VoiceFlow
        CFBundleShortVersionString: "0.1.0"
        CFBundleVersion: "1"
        UILaunchScreen:
          UIColorName: LaunchBackground
        UISupportedInterfaceOrientations:
          - UIInterfaceOrientationPortrait
        UIBackgroundModes:
          - audio
        NSMicrophoneUsageDescription: "VoiceFlow needs microphone access to transcribe your speech locally on your device. Audio never leaves your phone."
        NSSpeechRecognitionUsageDescription: "VoiceFlow uses speech recognition to transcribe your voice."
        # Issue #27: register the voiceflow:// URL scheme so the keyboard
        # extension can deep-link into a recording session.
        CFBundleURLTypes:
          - CFBundleURLName: de.marvinschwab.voiceflow.scheme
            CFBundleTypeRole: Editor
            CFBundleURLSchemes:
              - voiceflow
        UIApplicationSceneManifest:
          UIApplicationSupportsMultipleScenes: false
        ITSAppUsesNonExemptEncryption: false
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: de.marvinschwab.voiceflow
        TARGETED_DEVICE_FAMILY: "1"  # iPhone only
        ENABLE_PREVIEWS: YES
        SWIFT_EMIT_LOC_STRINGS: YES
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
        ASSETCATALOG_COMPILER_GLOBAL_ACCENT_COLOR_NAME: AccentColor

  # ==========================================================================
  # Keyboard Extension Target
  # ==========================================================================
  VoiceFlowKeyboard:
    type: app-extension
    platform: iOS
    sources:
      - path: Sources/VoiceFlowKeyboard
      - path: Sources/VoiceFlowShared
    dependencies:
      - target: VoiceFlowShared
        embed: false
      - sdk: UIKit.framework
    info:
      path: Sources/VoiceFlowKeyboard/Info.plist
      properties:
        CFBundleDisplayName: VoiceFlow Keyboard
        CFBundleShortVersionString: "0.1.0"
        CFBundleVersion: "1"
        NSExtension:
          NSExtensionAttributes:
            IsASCIICapable: false
            PrefersRightToLeft: false
            PrimaryLanguage: en-US
            RequestsOpenAccess: true
          NSExtensionPointIdentifier: com.apple.keyboard-service
          NSExtensionPrincipalClass: $(PRODUCT_MODULE_NAME).KeyboardViewController
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: de.marvinschwab.voiceflow.keyboard
        TARGETED_DEVICE_FAMILY: "1"
        SKIP_INSTALL: YES

  # ==========================================================================
  # Shared Framework
  # ==========================================================================
  VoiceFlowShared:
    type: framework
    platform: iOS
    sources:
      - path: Sources/VoiceFlowShared
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: de.marvinschwab.voiceflow.shared
        DEFINES_MODULE: YES
        SKIP_INSTALL: YES

  # ==========================================================================
  # Unit Tests Target
  # ==========================================================================
  VoiceFlowTests:
    type: bundle.unit-test
    platform: iOS
    sources:
      - path: Tests/VoiceFlowTests
    dependencies:
      - target: VoiceFlow
      - target: VoiceFlowShared
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: de.marvinschwab.voiceflow.tests
        TEST_HOST: $(BUILT_PRODUCTS_DIR)/VoiceFlow.app/$(BUNDLE_EXECUTABLE_FOLDER_PATH)/VoiceFlow
        BUNDLE_LOADER: $(TEST_HOST)

  # ==========================================================================
  # UI Tests Target
  # ==========================================================================
  VoiceFlowUITests:
    type: bundle.ui-testing
    platform: iOS
    sources:
      - path: Tests/VoiceFlowUITests
    dependencies:
      - target: VoiceFlow
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: de.marvinschwab.voiceflow.uitests
        TEST_TARGET_NAME: VoiceFlow

# ============================================================================
# Schemes
# ============================================================================

schemes:

  VoiceFlow:
    build:
      targets:
        VoiceFlow: all
        VoiceFlowTests: [test]
        VoiceFlowUITests: [test]
    run:
      config: Debug
    test:
      config: Debug
      targets:
        - VoiceFlowTests
        - VoiceFlowUITests
    profile:
      config: Release
    analyze:
      config: Debug
    archive:
      config: Release

  VoiceFlowKeyboard:
    build:
      targets:
        VoiceFlowKeyboard: all
